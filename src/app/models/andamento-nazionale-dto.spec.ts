import { AndamentoNazionaleDto } from './andamento-nazionale-dto';

describe('AndamentoNazionaleDto', () => {
  it('should create an instance', () => {
    expect(new AndamentoNazionaleDto()).toBeTruthy();
  });
});
